# Images ARE tracked (full mirror). Each image is turned into a text 【图注】
# (AI vision caption) embedded in its article's .md, so the grounding skill can
# read image content as citable text. See scripts/caption_images.py.

# secrets / local scraper state
.auth
*.log
__pycache__/
.venv/
.DS_Store
